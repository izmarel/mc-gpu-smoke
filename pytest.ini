[pytest]
# `.` so `import src.memory` resolves; `tests` so `import conftest` resolves.
pythonpath = . tests
testpaths = tests
addopts = -ra
markers =
    slow: marks tests as slow (deselect with -m "not slow")